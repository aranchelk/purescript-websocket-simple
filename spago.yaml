package:
  dependencies:
    - arraybuffer-types
    - console
    - effect
    - enums
    - foreign
    - functions
    - invariant
    - maybe
    - nullable
    - prelude
    - unsafe-coerce
    - var
    - web-events
    - web-socket
  name: websocket-simple
workspace:
  packageSet:
    registry: 61.0.2
  extraPackages:
    var:
      git: ssh://git@github.com/aranchelk/purescript-var.git
      ref: master
