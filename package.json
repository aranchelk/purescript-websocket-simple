{
  "private": true,
  "scripts": {
    "build": "pulp build -- --censor-lib",
    "example": "pulp build -I example/src/ -- --censor-lib"
  },
  "dependencies": {
    "spago": "^0.20.9",
    "ws": "^6.1.2"
  },
  "devDependencies": {
    "pulp": "^12.2.0",
    "purescript": "^0.15.4",
    "purescript-psa": "^0.6.0"
  }
}
